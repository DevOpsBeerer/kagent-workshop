apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operators-control-center.fullname" . }}-test-connection"
  labels:
    {{- include "operators-control-center.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "operators-control-center.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
