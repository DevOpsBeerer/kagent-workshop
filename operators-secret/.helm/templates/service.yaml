apiVersion: v1
kind: Service
metadata:
  name: {{ include "operators-control-center.fullname" . }}
  labels:
    {{- include "operators-control-center.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "operators-control-center.selectorLabels" . | nindent 4 }}
