apiVersion: v1
kind: Service
metadata:
  name: mission-control
  namespace: artemis-uc3
  labels:
    app: mission-control
    kagent-workshop/uc: uc3
spec:
  type: ClusterIP
  selector:
    app: mission-control
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
