apiVersion: v1
kind: Service
metadata:
  name: mission-control
  namespace: artemis-uc2
  labels:
    app: mission-control
    kagent-workshop/uc: uc2
spec:
  type: ClusterIP
  selector:
    app: mission-control
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
