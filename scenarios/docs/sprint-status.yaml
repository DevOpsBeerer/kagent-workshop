# BMAD Method Sprint Status
# Generated: 2026-04-28T00:00:00Z
# Project: kagent-workshop-scenarios

project_name: "kagent-workshop-scenarios"
project_level: 2
sprint_plan_path: "docs/sprint-plan-kagent-workshop-scenarios-2026-04-28.md"
current_sprint: 6
last_updated: "2026-05-05T17:10:00Z"

# Sprint windows are milestone-aligned (M1 → M5 from PRD/brief).
# Capacity model: 2 senior devs, 6 productive h/day, 2 h per story point.

team:
  size: 2
  members:
    - "Clément Raussin"
    - "Quentin Rodic"
  default_uc_ownership:
    UC1: "Clément Raussin"
    UC2: "Quentin Rodic"
    UC3: "Clément Raussin"
    UC4: "joint"

sprints:
  - sprint_number: 1
    milestone: "M1"
    goal: "Lock the conventions before parallel UC work starts: schemas vendored, FastAPI skeleton + variants ready, author dev loop functional, root docs and CODEOWNERS in place."
    start_date: "2026-04-28"
    end_date: "2026-05-01"
    capacity_points: 30
    committed_points: 26
    completed_points: 24
    status: "completed"
    stories:
      - story_id: "STORY-001"
        title: "Initialize repo skeleton"
        epic: "EPIC-001"
        frs: ["FR-001"]
        points: 1
        owner: "joint"
        status: "completed"
        completion_date: "2026-04-28"
        actual_points: 1
      - story_id: "STORY-002"
        title: "Vendor workshop-tour schema + make validate-tours"
        epic: "EPIC-001"
        frs: ["FR-002"]
        points: 2
        owner: "joint"
        status: "completed"
        completion_date: "2026-04-28"
        actual_points: 2
      - story_id: "STORY-003"
        title: "Vendor kagent v0.9.0 CRDs + make lint-agents"
        epic: "EPIC-001"
        nfrs: ["NFR-005"]
        points: 2
        owner: "joint"
        status: "skipped"
        skip_reason: "Deferred from M1: kagent CRDs reach the cluster via the canonical OCI helm chart (oci://ghcr.io/kagent-dev/kagent/helm/kagent-crds) installed by `make kagent-install`; offline lint-agents is a nice-to-have, not a workshop blocker. M2 manifests get end-to-end-validated by `make uc<N>-up` against a real cluster instead. Re-evaluate before M5."
      - story_id: "STORY-004"
        title: "Root README"
        epic: "EPIC-001"
        frs: ["FR-003"]
        points: 3
        owner: "joint"
        status: "completed"
        completion_date: "2026-04-30"
        actual_points: 3
      - story_id: "STORY-005"
        title: "Author dev loop (preflight, kind, Makefile)"
        epic: "EPIC-001"
        frs: ["FR-004"]
        points: 5
        owner: "joint"
        status: "completed"
        completion_date: "2026-04-30"
        actual_points: 5
      - story_id: "STORY-006"
        title: "Artemis naming convention + lore index"
        epic: "EPIC-001"
        frs: ["FR-005"]
        points: 2
        owner: "joint"
        status: "completed"
        completion_date: "2026-04-30"
        actual_points: 2
      - story_id: "STORY-007"
        title: "Tour content convention doc"
        epic: "EPIC-001"
        frs: ["FR-006"]
        points: 1
        owner: "joint"
        status: "completed"
        completion_date: "2026-04-30"
        actual_points: 1
      - story_id: "STORY-008"
        title: "apps/_skeleton FastAPI base"
        epic: "EPIC-001"
        frs: ["FR-007"]
        points: 3
        owner: "joint"
        status: "completed"
        completion_date: "2026-04-30"
        actual_points: 3
      - story_id: "STORY-009"
        title: "apps/mission-control variant"
        epic: "EPIC-001"
        frs: ["FR-007"]
        points: 1
        owner: "joint"
        status: "completed"
        completion_date: "2026-04-30"
        actual_points: 1
      - story_id: "STORY-010"
        title: "apps/lunar-rover-telemetry variant (/leak + /metrics)"
        epic: "EPIC-001"
        frs: ["FR-007"]
        points: 3
        owner: "joint"
        status: "completed"
        completion_date: "2026-04-30"
        actual_points: 3
      - story_id: "STORY-011"
        title: "CODEOWNERS + gitleaks + lint-manifests CI hooks"
        epic: "EPIC-001"
        nfrs: ["NFR-008", "NFR-006", "NFR-011"]
        points: 3
        owner: "joint"
        status: "completed"
        completion_date: "2026-04-30"
        actual_points: 3

  - sprint_number: 2
    milestone: "M2"
    goal: "Deliver UC1 and UC2 in parallel — first two reproducible scenarios + tours + cross-author signed reproductions."
    start_date: "2026-05-04"
    end_date: "2026-05-08"
    capacity_points: 30
    committed_points: 20
    completed_points: 20
    # Implementation 6/6 done; cross-author repro sign-offs on STORY-014 (Quentin → was on Clément's slate)
    # and STORY-017 (Clément → reciprocal swap) remain deferred until Clément is back or until M5 dry-run.
    status: "completed"
    stories:
      - story_id: "STORY-012"
        title: "UC1 manifests + agent CRDs"
        epic: "EPIC-002"
        frs: ["FR-008"]
        points: 5
        owner: "Clément Raussin"
        status: "completed"
        completion_date: "2026-04-30"
        actual_points: 5
      - story_id: "STORY-013"
        title: "UC1 tour.json"
        epic: "EPIC-002"
        frs: ["FR-009"]
        points: 3
        owner: "Clément Raussin"
        status: "completed"
        completion_date: "2026-04-30"
        actual_points: 3
      - story_id: "STORY-014"
        title: "UC1 README + cross-author repro"
        epic: "EPIC-002"
        frs: ["FR-008"]
        nfrs: ["NFR-003"]
        points: 2
        owner: "Quentin Rodic"  # re-attributed: Clément OOO at start of M2; Quentin ghost-authored the README. Cross-author repro AC therefore lands on Clément when he is back.
        status: "completed"
        completion_date: "2026-05-03"
        actual_points: 2
      - story_id: "STORY-015"
        title: "UC2 manifests + bootstrap taint Job + agent CRDs"
        epic: "EPIC-002"
        frs: ["FR-010"]
        points: 5
        owner: "Quentin Rodic"
        status: "completed"
        completion_date: "2026-05-03"
        actual_points: 5
      - story_id: "STORY-016"
        title: "UC2 tour.json"
        epic: "EPIC-002"
        frs: ["FR-011"]
        points: 3
        owner: "Quentin Rodic"
        status: "completed"
        completion_date: "2026-05-03"
        actual_points: 3
      - story_id: "STORY-017"
        title: "UC2 README + cross-author repro"
        epic: "EPIC-002"
        frs: ["FR-010"]
        nfrs: ["NFR-003"]
        points: 2
        owner: "Quentin Rodic"
        status: "completed"
        completion_date: "2026-05-03"
        actual_points: 2
        # AC #2 (Clément runs UC2 repro on fresh kind + signs off) deferred until Clément is back
        # or until M5 dry-run (STORY-028) covers it. README authoring (AC #1) is complete.

  - sprint_number: 2.5
    milestone: "M2.5"
    goal: "Patch the M2 tour narrative: re-frame UC1/UC2 tours as Artemis missions with no upfront spoiler; UC1 agent invocation moves to the kagent UI/chat. Convention authoritative for UC3/UC4 to consume from M3 onward."
    start_date: "2026-05-05"
    end_date: "2026-05-08"
    capacity_points: 24
    committed_points: 14
    completed_points: 14
    status: "completed"
    stories:
      - story_id: "STORY-030"
        title: "Mission-framing tour convention update"
        epic: "EPIC-001"
        frs: ["FR-006"]
        nfrs: ["NFR-008", "NFR-009", "NFR-010"]
        points: 3
        owner: "joint"
        status: "completed"
        completion_date: "2026-05-04"
        actual_points: 3
      - story_id: "STORY-031"
        title: "UC1 tour rewrite + UI/chat invocation"
        epic: "EPIC-002"
        frs: ["FR-009"]
        nfrs: ["NFR-003", "NFR-008", "NFR-010"]
        points: 5
        owner: "Clément Raussin"
        status: "completed"
        completion_date: "2026-05-04"
        actual_points: 5
        # AC #2 (NFR-003 reproduction sign-off on the rewritten tour) deferred
        # to the PR / M5 dry-run — manifests are byte-identical to M2 so the
        # cluster broken state is unchanged; only the tour text + Beat 3
        # invocation method need a real kagent dashboard walk-through.
      - story_id: "STORY-032"
        title: "UC2 tour rewrite (mission framing, CLI invoke retained)"
        epic: "EPIC-002"
        frs: ["FR-011"]
        nfrs: ["NFR-003", "NFR-008", "NFR-010"]
        points: 3
        owner: "Quentin Rodic"
        status: "completed"
        completion_date: "2026-05-04"
        actual_points: 3
        # Blocked by STORY-030; independent of STORY-031 once convention is in.
        # Manifests + agent CRDs unchanged from M2 (bit-for-bit). Tour rewritten
        # under STORY-030's 4-beat convention: Beat 1 mission setup (no spoiler),
        # Beat 2 single `kubectl get pods` friction signal, Beat 3 retains CLI
        # invoke (kagent invoke …), Beat 4 manual recap of the five pod-side +
        # node-side commands. AC #2 (NFR-003 reproduction sign-off on the
        # rewritten tour) deferred to the PR / M5 dry-run — manifests are
        # byte-identical to M2 so the cluster broken state is unchanged; only
        # the tour text needs a real cold-deploy walk-through.
      - story_id: "STORY-033"
        title: "UC0 — Install kagent (prep tour)"
        epic: "EPIC-001"
        frs: ["FR-006"]
        nfrs: ["NFR-008", "NFR-009", "NFR-010", "NFR-011"]
        points: 3
        owner: "joint"
        status: "completed"
        completion_date: "2026-05-04"
        actual_points: 3
        # Spike on `kagent install` profiles: `--profile demo` chosen so UC3
        # finds the pre-packaged Prom/Grafana agents already wired (architecture
        # L300). Tour is 4 steps under the new "Prep tours" convention exception
        # (no 4-beat — UC0 has no diagnostic friction to surface). Provider is
        # OpenAI per UC1's M2 ModelConfig; namespace is `kagent` (CLI default).
        # Live walk-through on a real kagent v0.9.0 install deferred to the PR /
        # M5 dry-run.

  - sprint_number: 3
    milestone: "M3"
    goal: "Deliver UC3 with on-the-fly Grafana dashboard, and unblock UC4 by completing the MCP source and packaging in parallel. Also close the narrative axis: scrub UC/meta-references from UC0/UC1/UC2 tour prose so STORY-020 (UC3) inherits a clean convention."
    start_date: "2026-05-11"
    end_date: "2026-05-13"
    capacity_points: 30
    committed_points: 25
    completed_points: 25
    status: "not_started"
    # STORY-034 (2 pts) implemented out-of-band on 2026-05-05 — ahead of sprint start
    # so the updated convention is in place before STORY-020 (UC3 tour) is authored.
    # STORY-018 (5) + STORY-019 (5) + STORY-020 (3) + STORY-021 (2) implemented out-of-band
    # on 2026-05-08 — full M3 EPIC-003 sweep, all four originally Clément's slate, all
    # taken by Quentin during the OOO window to keep the M3 critical path open.
    # STORY-022 (5) + STORY-023 (3) implemented out-of-band on 2026-05-08 — Quentin's
    # ORIGINAL M3 slate (no swap context). MCP source + packaging closes EPIC-004's
    # M3 deliverables completely.
    # Sprint formally starts 2026-05-11. **All 25 committed Sprint 3 points are landed
    # before formal sprint start.** Remaining Sprint 3 commitments: zero. Cross-author
    # repro deferrals from the swap chain are tracked under STORY-028 (M5 dry-run).
    #
    # Recursive cross-author-repro deferral chain (NFR-008 — "PR cross-reviewed by the
    # other author before merge" cannot be self-satisfied):
    #   STORY-014/017/031/032/033/034/018/019/020 → all deferred to "STORY-021's repro
    #   pass / M5 dry-run STORY-028" while authoring.
    #   STORY-021 itself swapped to Quentin → its own NFR-008 review defers to STORY-028.
    #   STORY-028's plate at M5 entry: cross-author cold-deploy NFR-003 + NFR-008 review
    #   of UC0/UC1/UC2/UC3 tour prose + UC3 README + the four EPIC-003 manifests/agents.
    #   That's the full cost of the swap, transparently logged here.
    # STORY-020 narrative-quality risk note: Clément retained narrative-judgment slot
    # before this swap (per STORY-019 status note). Tonal sign-off on uc3/tour.json
    # absorbed by STORY-028 as well — Clément may rewrite Beat-3/4 prose without cluster
    # blast radius (pure-text follow-up).
    stories:
      - story_id: "STORY-018"
        title: "infra/observability/ Prom + Graf bundle"
        epic: "EPIC-003"
        frs: ["FR-012"]
        nfrs: ["NFR-002", "NFR-006"]
        points: 5
        owner: "Quentin Rodic"  # re-attributed: Clément OOO at start of M3; Quentin took
                                # the bundle so STORY-019/020/021 (Clément's slate, all
                                # `Deps: STORY-018`) aren't blocked when he's back.
                                # Cross-author repro AC therefore lands on Clément when
                                # he is back — bundled with STORY-021's repro pass.
        status: "completed"
        completion_date: "2026-05-08"
        actual_points: 5
        # All AC validated against a live kind cluster (kagent-workshop): Prom + Graf
        # both Ready 1/1 in 31s; `Prometheus Server is Ready.`; Grafana /api/health OK
        # (db ok, v11.3.0); provisioned Prom datasource present + isDefault: true +
        # `Successfully queried the Prometheus API.` from the /api/datasources/uid/.../health
        # probe; sentinel Service with monitoring=prom picked up by kubernetes_sd within
        # 20s with 32 droppedTargets confirming the relabel filter; re-apply reports every
        # object `unchanged` (idempotent); make lint-manifests clean over uc*/manifests/
        # and infra/observability/. `kubectl top` AC skipped — metrics-server not in this
        # kind cluster (pre-existing kind setup gap, not a STORY-018 blocker).
        # Cross-author repro by Clément deferred to STORY-021's repro pass / M5 dry-run
        # STORY-028 — same pattern as STORY-014/017/031/032/033/034.
        # Out-of-scope finding flagged for follow-up: `make kagent-install` uses
        # `helm install` not `helm upgrade --install`, so re-running it on a cluster
        # that already has kagent fails with "cannot re-use a name that is still in use".
        # `make uc3-up` therefore can't bring the bundle up cleanly until kagent-install
        # is made idempotent; for STORY-018 validation the bundle was applied directly
        # via `kubectl apply -k infra/observability/`. Tracking under STORY-035 candidate
        # / sprint-3 retro.
      - story_id: "STORY-019"
        title: "UC3 manifests + agent CRDs"
        epic: "EPIC-003"
        frs: ["FR-012"]
        nfrs: ["NFR-001", "NFR-003"]
        points: 5
        owner: "Quentin Rodic"  # re-attributed: same swap as STORY-018; Clément OOO at
                                # start of M3. STORY-019 absorbed the M3 day-1 spike
                                # (kagent v0.9.0 pre-packaged Prom/Graf agent CRD shape)
                                # so STORY-020/021 inherit the trace. Cross-author repro
                                # AC therefore lands on Clément when he is back —
                                # bundled with STORY-021's repro pass.
        status: "completed"
        completion_date: "2026-05-08"
        actual_points: 5
        # All AC validated against the live `kagent-workshop` kind cluster:
        # - uc3/manifests/ apply cleanly; lunar-rover-telemetry Pod Running 1/1
        #   after the lunar-rover-telemetry:v1.0.0 image was published mid-validation
        #   (image-publish dependency flagged in story doc; user published it on
        #   request, validation continued).
        # - Prometheus kubernetes_sd discovered the monitoring=prom-labelled Service
        #   within ~20s; process_resident_memory_bytes{kubernetes_namespace="artemis-uc3",
        #   kubernetes_service_name="lunar-rover-telemetry"} queryable end-to-end.
        # - 11 POST /leak calls drove the container into OOMKilled (exit code 137);
        #   kubelet restarted, lastState.terminated.reason: OOMKilled now visible to
        #   the agent's k8s_describe_pod tool. (Spec said ~70 calls; idle RSS was
        #   already ~57MB so the 64Mi limit was reached faster — same friction signal.)
        # - Agent CRD `artemis-rover-telemetry-debugger` accepted by kagent v0.9.0
        #   (a2a sub-agent shape via tools[].type: Agent confirmed against
        #   observability-agent.spec example), READY=True + ACCEPTED=True within 60s.
        # - ExternalName bridge Services in kagent ns resolve correctly:
        #   grafana.kagent.svc → grafana.artemis-observability.svc (10.96.128.166);
        #   prometheus.kagent.svc → prometheus-server.artemis-observability.svc
        #   (10.96.144.249). Verified via busybox DNS test pod.
        # - make lint-manifests clean over uc1+uc2+uc3 manifests + infra/observability.
        # End-to-end agent invocation (system prompt → describe-pod → delegate to
        # observability-agent → dashboard URL) NOT validated — requires live OpenAI
        # creds. Deferred to STORY-021 cross-author repro / M5 dry-run STORY-028.
        # Cross-author repro by Clément deferred to STORY-021's repro pass / M5 dry-run
        # STORY-028 — same pattern as STORY-014/017/031/032/033/034.
        # M3 day-1 spike (kagent pre-packaged Prom/Graf agent CRD shape) absorbed by
        # this story's spike phase: actual agent names are `promql-agent` (PromQL
        # generator only) and `observability-agent` (Grafana + Prom execution + a2a
        # delegate to promql-agent). Architecture's "Prometheus and Grafana agents"
        # text → these two. RemoteMCPServers `kagent-tool-server` and
        # `kagent-grafana-mcp` provide the tool surfaces. Full trace in
        # docs/stories/STORY-019.md §Spike findings.
      - story_id: "STORY-020"
        title: "UC3 tour.json (leak loop + dashboard URL)"
        epic: "EPIC-003"
        frs: ["FR-013"]
        nfrs: ["NFR-008", "NFR-009", "NFR-010"]
        points: 3
        owner: "Quentin Rodic"  # re-attributed: third Clément story in the M3 swap.
                                # First swap into the narrative axis (UC3 tour prose) —
                                # contradicts the STORY-019 status note that said
                                # "Clément retains ownership of STORY-020". Tonal
                                # sign-off therefore upgraded into STORY-021's cross-
                                # author repro AC. Pure-prose follow-up has zero
                                # cluster blast radius (matches STORY-031/032 →
                                # STORY-034 precedent).
        status: "completed"
        completion_date: "2026-05-08"
        actual_points: 3
        # All AC validated end-to-end against the live `kagent-workshop` kind cluster:
        # - `make validate-tours` clean over uc0/uc1/uc2/uc3 (4/4 schema-valid).
        # - Custom audit (case-sensitive `UC<N>` regex per STORY-034 precedent):
        #   zero matches for banned vocab / bug-class names / meta-references across
        #   10 participant-visible fields (tour.title, tour.description, 4× step.title,
        #   4× step.explanation).
        # - Beat 1 chain (apply → wait Ready → telemetry stream) executes in ~3s,
        #   stream pod auto-deletes (`kubectl run --rm`), survives rover OOM cycles
        #   (loop runs against the in-cluster Service DNS).
        # - Beat 2 (`kubectl get pods`) shows the exact friction signal: Running 1/1
        #   with `RESTARTS: 1 (Ns ago)`. lastState.terminated.reason == "OOMKilled"
        #   confirmed via jsonpath probe — that's what the agent's k8s_describe_pod
        #   tool will surface in Beat 3.
        # - Prometheus still scraping the post-restart rover (~49 MiB working set on
        #   the new container as of validation; ready for the agent's PromQL panel).
        # Implementation finding: container has only `python3`, no `curl`/`wget`.
        # Switched the leak-trigger from `kubectl exec ... -- curl ...` (which fails
        # with `sh: 1: curl: not found`) to `kubectl run --rm` with curlimages/curl.
        # Bonus: the run-pod survives rover restarts so the 30-burst loop actually
        # runs to completion (the exec form would die on the first OOM at exit 137).
        # The tour's mission framing absorbs this cleanly — the run-pod is "a transient
        # operator station that pushes 30 bursts to the rover's outbound queue".
        # Agent reasoning loop end-to-end (Beat 3 prompt → describe-pod → delegate to
        # observability-agent → dashboard URL surfaced) NOT validated — needs live
        # OpenAI creds. Deferred to STORY-021's repro pass / M5 dry-run STORY-028.
        # Cross-author repro by Clément deferred to STORY-021's repro pass / M5 dry-run
        # STORY-028. Tonal sign-off explicitly upgraded into that pass per the
        # narrative-quality risk note in docs/stories/STORY-020.md.
      - story_id: "STORY-021"
        title: "UC3 README + cross-author repro"
        epic: "EPIC-003"
        frs: ["FR-012"]
        nfrs: ["NFR-003", "NFR-008"]
        points: 2
        owner: "Quentin Rodic"  # re-attributed: fourth and final M3 Clément swap.
                                # Sprint plan AC #2 originally read "Quentin runs UC3
                                # reproduction once on fresh kind and signs off" —
                                # encoded for Clément's authorship. With the swap,
                                # the cross-review target inverts to Clément, whose
                                # NFR-008 signoff defers to STORY-028 (M5 dry-run).
                                # Recursive deferral chain documented in this sprint's
                                # comment block above + in STORY-021.md §Ownership swap.
        status: "completed"
        completion_date: "2026-05-08"
        actual_points: 2
        # All AC validated:
        # - uc3/README.md rewritten from stub to UC1/UC2 convention shape (~470 lines)
        #   with the two UC3-specific sections AC #1 names: Prom/Graf prerequisite +
        #   bridge (cross-references STORY-018 bundle + STORY-019 bridge Services) and
        #   the production-disclaimer (one-paragraph rationale per "what's pedagogical
        #   here" / "what you would change for a production-grade install"). Plus a
        #   UC3-specific Recovery procedure section (the OOM cycle is participant-
        #   triggered, so re-running the tour bumps RESTARTS — Recovery levels 1-4
        #   document the reset paths from "ad-hoc rerun" to "full kind teardown").
        # - Author notes section captures three engineering-trace items per STORY-034
        #   convention: kagent v0.9.0 pre-packaged agent CRD shape spike (cross-
        #   references STORY-019.md §Spike findings); kubectl run --rm leak-trigger
        #   pattern (STORY-020 finding — no curl in python:3.12-slim container);
        #   ExternalName bridge rationale (DNS-CNAME indirection vs helm-values
        #   override decision matrix).
        # - Self-author smoke validation against the live `kagent-workshop` kind cluster
        #   ran the README-documented checks (a)-(d) verbatim:
        #     (a) restartCount = 1 — README's `≥ 1` predicate satisfied.
        #     (b) lastState.terminated.reason == "OOMKilled" — confirmed.
        #     (c) Prom scraping artemis-uc3: process_resident_memory_bytes value
        #         53.7 MiB on the post-restart container.
        #     (d) Bridge DNS resolution from a busybox pod in the kagent ns:
        #         grafana.kagent.svc → grafana.artemis-observability.svc;
        #         prometheus.kagent.svc → prometheus-server.artemis-observability.svc.
        #   Idempotency: re-applying uc3/manifests/ + uc3/agents/ reports `unchanged`
        #   for all 7 resources. `make validate-tours` clean (4/4 schema-valid).
        # - Cross-author repro by Clément (NFR-008 + NFR-003 cold-deploys): DEFERRED to
        #   STORY-028 (M5 dry-run). Self-review is not cross-review; the swap left
        #   Quentin as both author and only available reviewer at story-completion time.
        #   Pure-text follow-up rewrites by Clément have zero cluster blast radius
        #   (manifests/agents/tour stay byte-identical; only README prose changes).
        # - Agent reasoning loop end-to-end (Beat 3 invocation → describe-pod → delegate
        #   → dashboard URL) NOT validated — needs OpenAI creds. Same chain as STORY-019/
        #   020; defers to STORY-028.
      - story_id: "STORY-022"
        title: "KMCP MCP source + tenancy guard + tests"
        epic: "EPIC-004"
        frs: ["FR-016"]
        nfrs: ["NFR-012"]
        points: 5
        owner: "Quentin Rodic"  # ORIGINAL slate — first M3 story on Quentin's plate
                                # without an ownership swap. No deferral chain, no
                                # narrative-quality risk, no recursive cross-author
                                # repro complications. Contrasts with STORY-018/019/
                                # 020/021 which were all swap targets from Clément.
        status: "completed"
        completion_date: "2026-05-08"
        actual_points: 5
        # All AC validated:
        # - mcp/ scaffolded by `kmcp init python mcp` (kmcp v0.2.8); kmcp.yaml,
        #   pyproject.toml, src/main.py, src/core/{server,utils}.py, src/tools/,
        #   tests/ all generated. Plus Dockerfile (consumed by STORY-023).
        # - kmcp.yaml.name renamed `mcp` → `artemis-bulb-mcp` per artemis-naming.md L72.
        # - kmcp-default echo tool deleted; kmcp-generated tests/ updated to drop echo
        #   assertions and assert list_bulbs + update_bulb instead.
        # - pyproject.toml gained `httpx==0.28.1` for the light-manager HTTP client.
        # - src/core/models.py: vendored BulbRead, BulbUpdate (r/g/b ge=0/le=255 via
        #   Pydantic Field), VALID_SLOTS=(1,2,3) — byte-equivalent to
        #   light-manager/backend/app/routers/bulbs.py:17-30 with provenance comment.
        # - src/core/tenancy.py: WORKSHOP_PARTICIPANT_LOGIN env-var guard with custom
        #   TenancyMismatchError(PermissionError) class; fail-closed on unset/empty env.
        # - src/core/lightmanager_client.py: httpx-based, env-driven LIGHT_MANAGER_URL,
        #   GET /api/bulbs?user= and PUT /api/bulbs/{slot}?user= functions.
        # - src/tools/list_bulbs.py + update_bulb.py: tenancy guard runs before any HTTP
        #   call; update_bulb pre-validates slot ∈ VALID_SLOTS and constructs BulbUpdate
        #   (which Pydantic-validates r/g/b 0–255).
        # - 4 new test files (test_models.py, test_tenancy.py, test_list_bulbs.py,
        #   test_update_bulb.py) covering all 4 AC-named scenarios:
        #     happy path / slot validation / RGB validation / tenancy mismatch.
        #   Plus the kmcp-generated test_server.py + test_discovery.py (updated to drop
        #   echo references) + test_tools.py (rewritten for the bulb tool surface).
        # - `uv run pytest tests/` reports 61 passed, 0 failed in 1.03s.
        # - Tenancy AC: `grep -rn '"alice"\|"operator-0\|"bob"' src/` returns ZERO
        #   matches; the only WORKSHOP_PARTICIPANT_LOGIN reference in src/ is the env
        #   variable name itself (in tenancy.py) — confirms "no hard-coded user="
        #   per FR-016 + NFR-012.
        # - tests assert TenancyMismatchError fires *before* any HTTP call (mock
        #   `assert_not_called()`) — proves the guard is in front of the wire, not
        #   downstream of it.
        # No deferred AC. No live-cluster integration test (out of scope per story doc;
        # STORY-027 / STORY-028 cover live UC4 integration). No cross-author repro
        # required at this story's scope (NFR-008 is implicit at the M5 dry-run).
        # Sprint-3 retro candidate: codify a CI grep/diff hook against the vendored
        # Pydantic shapes vs light-manager source — would catch shape drift at PR time.
      - story_id: "STORY-023"
        title: "MCP packaging — Dockerfile + manifests + RemoteMCPServer"
        epic: "EPIC-004"
        frs: ["FR-016"]
        nfrs: ["NFR-002", "NFR-006"]
        points: 3
        owner: "Quentin Rodic"  # ORIGINAL slate. Sprint plan AC mentioned `ToolServer`
                                # CRD; STORY-023 ships `RemoteMCPServer` (kagent.dev/
                                # v1alpha2) which is what kagent v0.9.0 actually uses
                                # for STREAMABLE_HTTP and what UC1 already references.
                                # Title updated to reflect the actual CRD shipped.
        status: "completed"
        completion_date: "2026-05-08"
        actual_points: 3
        # All AC validated:
        # - mcp/Dockerfile adjusted from kmcp scaffold: CMD now defaults to FastMCP
        #   HTTP transport on 0.0.0.0:8080 (kmcp default was stdio); HEALTHCHECK
        #   replaced with a urllib-based GET against /mcp (kmcp default was just
        #   `import src.main`, which only verifies module-loadability).
        # - 4 manifests under mcp/manifests/: 00-namespace.yaml (artemis-mcp ns),
        #   10-deployment.yaml (single-replica, envFrom Secret + ConfigMap, TCP
        #   probes on the mcp port), 20-service.yaml (ClusterIP :8080), and
        #   30-remotemcpserver.yaml (kagent.dev/v1alpha2 RemoteMCPServer in the
        #   kagent ns, STREAMABLE_HTTP, full DNS URL pointing at the Service).
        # - Makefile gained mcp-build, mcp-up, mcp-down phony targets + a
        #   lint-manifests walk-extension over mcp/manifests/. `make help` updated.
        # - `make lint-manifests` clean across uc*/manifests/ + infra/observability/
        #   + mcp/manifests/ (server-side dry-run accepted by the live cluster's
        #   RemoteMCPServer CRD schema — substitutes for the missing make lint-agents
        #   that STORY-003 was supposed to ship; STORY-003 was skipped in M1).
        # - Self-author cluster smoke validated against live `kagent-workshop` kind:
        #     `make mcp-build`: image built `rg.fr-par.scw.cloud/apogasa/artemis-bulb-mcp:v0.1.0`.
        #     `kind load`: image side-loaded into the kagent-workshop control-plane node.
        #     Throwaway Secret/ConfigMap created (WORKSHOP_PARTICIPANT_LOGIN=operator-test,
        #       LIGHT_MANAGER_URL=http://light-manager.light-manager.svc.cluster.local:8000).
        #     `make mcp-up`: 4 resources applied cleanly.
        #     Pod Ready 1/1 in 11s of apply.
        #     FastMCP boot logs confirm Streamable HTTP server up at 0.0.0.0:8080/mcp.
        #     curl POST /mcp with MCP `initialize` JSON-RPC: returned valid SSE
        #       response (protocolVersion 2024-11-05, serverInfo.name=mcp).
        #     RemoteMCPServer initially landed Accepted=False due to a 7-second race
        #       between Pod creation and reconciler probe (Pod start 14:44:20, probe
        #       14:44:13). Annotation poke at 14:45:00 triggered immediate re-reconcile.
        #       60s later the next reconcile would have succeeded automatically; the
        #       poke just sped it up. Worth flagging in workshop-infrastructure docs:
        #       initial apply may transiently show Accepted=False; retry is automatic.
        #     After re-reconcile: `Accepted=True`, `discoveredTools=[list_bulbs, update_bulb]`
        #       — both tools' full docstrings (including the WORKSHOP_PARTICIPANT_LOGIN
        #       contract) flow through to kagent's status.
        #     `make mcp-down`: removes all 4 resources cleanly. Throwaway Secret +
        #       ConfigMap deleted; namespace deleted; cluster left pristine.
        # - No committed secrets (gitleaks-equivalent grep returns zero matches in
        #   committed source). The Deployment's envFrom references resources that
        #   only exist in the cluster, never in the repo.
        # No deferred AC. STORY-023 closes Sprint 3 commitments (25/25 points landed).
        # Sprint-3 retro candidate already flagged: workshop-infrastructure should
        # document the transient Accepted=False window so ops doesn't see it as a
        # bug; the auto-retry resolves within 60s.
      - story_id: "STORY-034"
        title: "Scrub UC/meta-references from tour prose (UC0, UC1, UC2) + convention rule update"
        epic: "EPIC-002"  # bundled; UC0 + convention slices are EPIC-001 spillover (see story doc)
        frs: ["FR-006", "FR-009", "FR-011"]
        nfrs: ["NFR-008", "NFR-009", "NFR-010"]
        points: 2
        owner: "Quentin Rodic"
        status: "completed"
        completion_date: "2026-05-05"
        actual_points: 2
        # Created and implemented out-of-band on 2026-05-05, ahead of Sprint 3 start
        # (2026-05-11) so STORY-020 (UC3 tour, Clément) inherits the updated convention.
        # All 11 leaked strings catalogued in the Leaks inventory have been scrubbed
        # from uc0/uc1/uc2 tour.json; audit notes (STORY-031/033 spike forms,
        # --profile demo rationale, Beat 3 invocation forms) relocated to per-UC
        # README.md Author notes sections; convention doc gained the
        # "No meta-references in prose" sub-rule and a leak-free Good example.
        # `make validate-tours` green, custom audit confirms zero meta-references in
        # participant-visible fields. Cross-author repro by Clément deferred to the
        # M5 dry-run (STORY-028) per the same pattern as STORY-031/032/033 — purely
        # textual change, no cluster behaviour impact.

  - sprint_number: 4
    milestone: "M4"
    goal: "Deliver UC4 — multi-symptom cluster, a2a coordinator, custom MCP integrated end-to-end, bulbs visibly change colour."
    start_date: "2026-05-13"
    end_date: "2026-05-17"
    capacity_points: 24
    committed_points: 14
    completed_points: 14
    status: "completed"
    # STORY-024 (3 pts) implemented out-of-band on 2026-05-08, joint→Quentin
    # re-attribution (fifth swap of the OOO window). Mechanical lift of UC1+UC2+UC3
    # patterns into a single namespace; cross-author repro absorbed by STORY-028.
    # STORY-025 (5 pts) implemented out-of-band on 2026-05-11 (formal Sprint 3
    # day-1), joint→Quentin re-attribution (sixth swap of the OOO window). UC4
    # coordinator a2a Agent CRD + UC2 drift inline patch (namespace + MCP server
    # name + modelConfig fixes) + bridge-Services promotion from uc3/agents/ to
    # infra/observability/. Cross-author repro absorbed by STORY-028.
    # STORY-026 (3 pts) implemented out-of-band on 2026-05-11 (same day as
    # STORY-025), joint→Quentin re-attribution (seventh swap of the OOO window).
    # UC4 tour.json — 4-beat shape per post-STORY-034 convention; CLI invoke
    # variant; bulb-colour-as-output folded into Beat 3 explanation per FR-017;
    # ?user=<login> visibility per FR-015. Leak trigger uses kubectl exec
    # in-pod-loopback (rover tolerates the taint; transient kubectl run pod
    # would not, per STORY-024 finding). Beat 1 → Beat 2 cluster-smoke
    # validated; end-to-end Beat 3 (live kagent invoke + bulb writes) deferred
    # to STORY-027/028 per the recursive deferral chain.
    # STORY-027 (3 pts) implemented out-of-band on 2026-05-11 (same day as
    # STORY-025/026), joint→Quentin re-attribution (eighth swap of the OOO
    # window — closes EPIC-004 authoring). UC4 README (403 lines) replacing
    # the 8-line stub: prerequisites + multi-symptom-mess table + coordination
    # flow walkthrough + FR-017 mapping tables + tenancy guarantee + files
    # index + 10-step NFR-003 reproduction checklist + Levels 1-4 recovery
    # + production disclaimer + Author notes consolidating six upstream
    # stories' engineering-trace items + 8-item Sprint-3 retro queue.
    # All 22 internal links resolve. Quentin self-author cluster smoke
    # verified all README claims against live state. Cross-author cold-deploy
    # by Clément deferred to STORY-028 (M5 dry-run) per the recursive
    # deferral chain — Clément's M5 plate now consolidated in STORY-027's
    # Technical Notes.
    # Sprint formally starts 2026-05-13 (~2 days out); all 14 / 14 committed
    # pts landed pre-launch — Sprint 4 fully complete.
    stories:
      - story_id: "STORY-024"
        title: "UC4 multi-symptom manifests"
        epic: "EPIC-004"
        frs: ["FR-014"]
        nfrs: ["NFR-001", "NFR-002", "NFR-003"]
        points: 3
        owner: "Quentin Rodic"  # re-attributed: joint → Quentin (fifth swap of the
                                # OOO window). Mechanical lift of UC1+UC2+UC3
                                # patterns into a single namespace; no narrative
                                # judgment to defer to Clément. Cross-author repro
                                # absorbed by STORY-028 (M5 dry-run) per the
                                # recursive deferral chain.
        status: "completed"
        completion_date: "2026-05-08"
        actual_points: 3
        # All AC validated end-to-end against the live `kagent-workshop` kind cluster:
        # - 7 manifests under uc4/manifests/: 00-namespace, 10-rbac (bootstrap Job
        #   RBAC, mirrors UC2), 20-bootstrap-taint-job (taint + rollout-restart),
        #   30-services (3 ClusterIP, lunar-rover with monitoring=prom),
        #   40-deployment-imagepull (:v999), 50-deployment-pending (:v1.0.0,
        #   strategy.type: Recreate), 60-deployment-telemetry (:v1.0.0, 64Mi limit).
        # - Three symptoms simultaneously verified:
        #     mission-control-imagepull: phase=Pending, reason=ImagePullBackOff
        #     mission-control-pending:   phase=Pending, reason=Pending (untolerated taint)
        #     lunar-rover-telemetry:     phase=Running, restarts=1, lastState.OOMKilled
        #   (last symptom requires a participant-triggered leak loop; STORY-026 ships
        #   the tour-side trigger.)
        # - Prom scraping artemis-uc4: process_resident_memory_bytes value 54.4 MiB
        #   on the post-restart container. Bridge to STORY-018 confirmed.
        # - make lint-manifests clean across uc1/uc2/uc3/uc4/manifests/ +
        #   infra/observability/ + mcp/manifests/.
        #
        # Three findings during validation (worth Sprint-3 retro):
        # 1. apogasa/mission-control:v1 (UC2 + UC4) and apogasa/kubectl:1.31.0 (UC2 +
        #    UC4 bootstrap) were NOT published. User mid-flight published
        #    mission-control:v1.0.0 and kubectl:latest. UC4 manifests now reference
        #    those tags; UC2 still references :v1 / :1.31.0 → UC2 likely never
        #    end-to-end validated on this cluster, latent gap.
        # 2. apogasa/kubectl:latest is busybox-based (Alpine), no /bin/bash. UC4's
        #    bootstrap Job switched from `["/bin/bash", …]` to `["/bin/sh", …]`
        #    (POSIX-compatible, busybox supports `set -o pipefail`). UC2's bootstrap
        #    Job still uses /bin/bash → would also break against the new image.
        # 3. UC2's bootstrap-taint Job lacks tolerations on its own pod, the imagepull
        #    Deployment, and the rover Deployment. UC4 added tolerations for all three
        #    (Job + imagepull + telemetry) so re-applies on a cluster with the
        #    pre-existing taint don't deadlock. UC2 relies on `make kind-down` between
        #    cold deploys per its README — UC4 is more robust to in-place re-applies.
        #    Default RollingUpdate strategy on the Pending Deployment also caused a
        #    deadlock (old Running pod sticks around because new Pending pod can never
        #    become Ready); UC4 uses `strategy.type: Recreate`. UC2 has the same latent
        #    issue.
        #
        # Sprint-3-retro candidates from STORY-024 specifically:
        #   - Update UC2's manifests with: image tags (:v1.0.0 + :latest), /bin/sh
        #     instead of /bin/bash, tolerations on the bootstrap Job, and Recreate
        #     strategy on the Pending Deployment. ~10-line patch, identical surgery
        #     to UC4's. (Out of scope for STORY-024 itself; UC2 is M2 territory.)
        #
        # Cross-author repro by Clément deferred to STORY-028 (M5 dry-run) per the
        # recursive deferral chain established in STORY-018/019/020/021.
      - story_id: "STORY-025"
        title: "UC4 coordinator a2a Agent CRD"
        epic: "EPIC-004"
        frs: ["FR-014", "FR-017"]
        nfrs: ["NFR-005", "NFR-008", "NFR-012"]
        points: 5
        owner: "Quentin Rodic"  # re-attributed: joint → Quentin (sixth swap of the
                                # OOO window). Mechanical reuse of UC3's a2a +
                                # MCP tool-list shape; the narrative-judgement
                                # surface is bounded (FR-017 mapping is locked
                                # in artemis-naming.md L80-92). Cross-author
                                # repro absorbed by STORY-028 (M5 dry-run) per
                                # the recursive deferral chain.
        status: "completed"
        completion_date: "2026-05-11"
        actual_points: 5
        # All AC validated end-to-end against the live `kagent-workshop` kind cluster:
        # - uc4/agents/agent.yaml: artemis-mission-coordinator Agent CRD in kagent
        #   ns, 3 a2a sub-agent refs (UC1/UC2/UC3 specialists) + 2 RemoteMCPServer
        #   refs (kagent-tool-server for k8s_get_pod + k8s_get_resources; artemis-
        #   bulb-mcp for list_bulbs + update_bulb) + ~110-line system prompt
        #   encoding FR-017 slot/colour mapping + NFR-012 tenancy contract +
        #   delegation discipline.
        # - uc4/agents/modelconfig.yaml: per-UC artemis-llm slot in artemis-uc4 ns
        #   (matches UC1/UC2/UC3 pattern; coordinator itself uses default-model-config
        #   per same UC1/UC3 precedent).
        # - Coordinator reaches Accepted=True + Ready=True after MCP rmcps reconcile
        #   (same 7-second race STORY-023 documented; annotation poke retriggers).
        #   discoveredTools on artemis-bulb-mcp rmcps confirms list_bulbs +
        #   update_bulb both surfaced through to kagent.
        #
        # UC2 inline drift patch (prerequisite for a2a delegation, not a UC2
        # refactor) — three issues fixed in uc2/agents/agent.yaml:
        #   1. metadata.namespace: artemis-uc2 → kagent (kagent v0.9.0 a2a wiring
        #      requires same-namespace; same precedent UC1+UC3 follow).
        #   2. tools[].mcpServer: rewritten to RemoteMCPServer shape with
        #      apiGroup: kagent.dev + kind: RemoteMCPServer + name: kagent-tool-server
        #      (the previous name kagent-tools-k8s did not exist on kagent v0.9.0
        #      and would have failed at runtime — latent bug, third confirmation
        #      that UC2 was never end-to-end validated).
        #   3. declarative.modelConfig: artemis-llm → default-model-config (UC2
        #      was the outlier; UC1+UC3 already use kagent's installed default.
        #      Discovered during STORY-025 validation when UC2 agent post-namespace-
        #      move landed Accepted=False with "ModelConfig.kagent.dev artemis-llm
        #      not found" — the ModelConfig lives in artemis-uc2 ns, the Agent now
        #      lives in kagent ns, same-namespace lookup fails).
        # All three are 1-line diffs in a single file. UC2 agent post-patch reaches
        # Accepted=True + Ready=True.
        #
        # Bridge-Services promotion (deferred from STORY-019 / STORY-024):
        # - uc3/agents/kagent-bridge-services.yaml moved to
        #   infra/observability/kagent-bridge-services.yaml. Preamble updated to
        #   reflect shared (observability-bundle) ownership; label
        #   `kagent-workshop/uc: uc3` swapped for
        #   `kagent-workshop/component: observability`.
        # - Makefile observability-up extended with `kubectl apply -f
        #   $(OBSERVABILITY_DIR)/kagent-bridge-services.yaml` (separate from the
        #   kustomize apply because the kustomization's root namespace override
        #   would mangle the bridge's kagent namespace). observability-down extended
        #   symmetrically. lint-manifests extended with a separate dry-run for the
        #   bridge file.
        # - uc3/README.md repath edits (bridge section + file tree).
        # - uc3/agents/agent.yaml preamble comment repath.
        # - Both UC3 and UC4 inherit the bridge transparently via `make uc<N>-up`'s
        #   existing dependency on observability-up.
        #
        # Validation:
        # - make lint-manifests clean across uc1/uc2/uc3/uc4/manifests + infra/observability
        #   (kustomize + bridge file) + mcp/manifests.
        # - kubectl apply --dry-run=server -f uc{1,2,3,4}/agents/ clean (artemis-uc{2,4}
        #   namespaces created for the modelconfig dry-run; benign).
        # - Live cluster: UC2 + UC4 agents both Accepted=True + Ready=True.
        # - UC1 agent on cluster is Accepted=False due to pre-existing stale spec
        #   (references kagent-tools-k8s, not the current uc1/agents/agent.yaml's
        #   kagent-tool-server). NOT a STORY-025 regression — UC1 is M2 territory.
        #   Sprint-3 retro candidate: re-apply uc1/agents/ to pick up the corrected
        #   spec the repo already ships.
        #
        # End-to-end agent reasoning loop NOT validated in STORY-025 (requires live
        # OpenAI creds + the light-manager backend + the three UC clusters simultaneously
        # in their broken states). Deferred to STORY-027's repro pass / STORY-028
        # (M5 dry-run) per the recursive deferral chain established in STORY-018/019/
        # 020/021/024. NFR-008 cross-author sign-off on the system-prompt prose also
        # deferred to STORY-028 — pure-text follow-up.
        #
        # Sprint-3-retro candidates from STORY-025:
        #   1. UC2 modelConfig was the third UC2 drift item; full UC2 patch (also UC2
        #      manifest tags + bootstrap shell + RollingUpdate + tolerations from
        #      STORY-024) is now a ~10-line Sprint-3-retro patch ahead of M5 dry-run.
        #   2. UC1 stale spec on cluster (Accepted=False) — single re-apply of
        #      uc1/agents/ fixes it.
        #   3. kagent v0.9.0 a2a runtime parallelism semantics — system prompt hedges
        #      ("delegate in parallel where the runtime supports it") because we
        #      haven't confirmed whether tools[].type: Agent calls are issued in
        #      parallel by the kagent reasoning loop. STORY-028 timing observations
        #      will surface this.
      - story_id: "STORY-026"
        title: "UC4 tour.json"
        epic: "EPIC-004"
        frs: ["FR-015", "FR-006", "FR-017"]
        nfrs: ["NFR-008", "NFR-009", "NFR-010", "NFR-012"]
        points: 3
        owner: "Quentin Rodic"  # re-attributed: joint → Quentin (seventh swap of
                                # the OOO window). Narrative-judgement surface
                                # bounded by post-STORY-034 4-beat convention +
                                # FR-017 mapping locked in artemis-naming.md
                                # L80-92. Cross-author tonal sign-off absorbed
                                # by STORY-028 (M5 dry-run) per the recursive
                                # deferral chain.
        status: "completed"
        completion_date: "2026-05-11"
        actual_points: 3
        # All AC validated:
        # - uc4/tour.json: 4-beat shape per post-STORY-034 convention. id
        #   kagent-uc4-coordinator; title "UC4 — The Artemis fleet check-in
        #   won't come together"; mission-framing description naming the
        #   coordinator's role + the status-bulb panel.
        # - Beat 1: 7 fileEdits byte-identical to on-disk uc4/manifests/*.yaml
        #   (including author comments per UC2/UC3 precedent + convention L59),
        #   3 commands[] (apply + wait Ready + leak trigger). Leak trigger uses
        #   kubectl exec into the rover Pod's in-pod loopback (rover tolerates
        #   the taint; transient kubectl run pod would not — per STORY-024's
        #   "STORY-026 will design the tour-side trigger appropriately"
        #   finding). 2>/dev/null || true swallows the exit code 137 the
        #   exec stream emits when the rover OOMs mid-loop.
        # - Beat 2: 1 commands[] entry (kubectl get pods -n artemis-uc4).
        #   Single-step legible — three workloads with three different
        #   STATUS/RESTARTS signals all show in one listing.
        # - Beat 3: CLI invoke variant per docs/tour-content-conventions.md
        #   § Beat 3 invocation. kagent invoke against
        #   artemis-mission-coordinator in kagent ns. Light-manager URL pattern
        #   with ?user=<your-login> surfaced in the explanation per FR-015 AC;
        #   bulb-colour-as-output framing per FR-017 (slot 1↔UC1, 2↔UC2,
        #   3↔UC3; red/green/amber per artemis-naming.md L80-92).
        # - Beat 4: pure markdown. Three diagnostic chains (~11 kubectl across
        #   two resource kinds, plus a Grafana panel for the OOM curve) +
        #   a fourth block with 3 curl PUT calls to light-manager. No
        #   commands[]/fileEdits. No cross-UC roadmap tail (UC4 is the
        #   climax — per convention's no-meta-references sub-rule).
        # - make validate-tours clean across 5/5 tour files (uc0/uc1/uc2/uc3/uc4).
        # - Custom no-spoiler audit on pre-Beat-4 participant-visible fields:
        #   0 issues (no banned vocab, no bug-class names, no UC<N> meta-refs,
        #   no STORY-... refs, no Beat <N> refs in explanations).
        # - fileEdits byte-identity check: all 7 contents byte-identical to
        #   on-disk uc4/manifests/*.yaml (verified via diff after first-draft
        #   accidentally stripped author comments).
        # - End-to-end Beat 1 → Beat 2 cluster-smoke validated on live
        #   kagent-workshop kind: apply succeeds, rover Ready after ~5s, leak
        #   trigger drives rover into OOMKilled (lastState.terminated.reason
        #   confirmed; exitCode 137); kubectl get pods shows three friction
        #   signals simultaneously (lunar-rover-telemetry Running 1/1
        #   RESTARTS: 1; mission-control-imagepull ImagePullBackOff;
        #   mission-control-pending Pending).
        #
        # Implementation findings (Sprint-3 retro candidates):
        # 1. First draft accidentally comment-stripped fileEdits content;
        #    re-embedded verbatim. Candidate: extend make validate-tours (or a
        #    sibling make audit-tours) with a fileEdits-byte-identity check.
        # 2. kubectl exec exit-code-137 ergonomics: terminal is silent for
        #    ~15s during the leak loop. Optional: prepend an echo for clearer
        #    progress reporting.
        # 3. Light-manager UI URL is participant-environment-supplied (Beat 3
        #    explanation says "workshop-infrastructure pre-wired the URL into
        #    your environment"). Confirm actual URL pattern with workshop-
        #    infrastructure team and tighten the explanation accordingly.
        #
        # Deferred AC (per recursive deferral chain):
        # - NFR-008 cross-author tonal sign-off on UC4 mission framing
        #   → STORY-028 (M5 dry-run).
        # - End-to-end Beat 3 execution (live kagent invoke → coordinator
        #   fans out → 3 bulbs flip in light-manager UI) → STORY-028.
        #   Requires OpenAI creds + light-manager backend reachable + the
        #   participant's bulb-panel URL reachable from a browser.
      - story_id: "STORY-027"
        title: "UC4 README + cross-author repro"
        epic: "EPIC-004"
        frs: ["FR-014", "FR-017"]
        nfrs: ["NFR-003", "NFR-008"]
        points: 3
        owner: "Quentin Rodic"  # re-attributed: joint → Quentin (eighth swap of
                                # the OOO window — closes EPIC-004 authoring).
                                # Narrative-judgement surface mostly *organising*
                                # six upstream stories' spike findings + design
                                # rationale, plus the UC4-unique FR-017 mapping
                                # tables. Cross-author cold-deploy by Clément
                                # absorbed by STORY-028 (M5 dry-run) per the
                                # recursive deferral chain.
        status: "completed"
        completion_date: "2026-05-11"
        actual_points: 3
        # All AC validated:
        # - uc4/README.md: 403-line README replacing the 8-line stub.
        #   Structure mirrors UC3 (288 lines) + FR-017 mapping tables UC1/2/3
        #   don't have. 15 sections (locked order):
        #     1. Header + framing paragraph.
        #     2. Artemis narrative.
        #     3. Prerequisites (4 sub-sections — kagent demo profile,
        #        observability bundle + bridge, sibling specialist Agent CRDs,
        #        custom bulb MCP).
        #     4. The multi-symptom mess (table of all 12 resources).
        #     5. Expected coordinator behaviour (three-layer tool surface +
        #        six-step Coordination flow walkthrough per sprint plan AC).
        #     6. Slot ↔ sub-agent mapping (FR-017 table).
        #     7. Verdict ↔ colour mapping (FR-017 table) + Expected output
        #        blockquote.
        #     8. Tenancy guarantee (FR-015 + NFR-012 + per-vCluster vs shared
        #        MCP trade-off).
        #     9. Files in this directory (tree + cross-refs to mcp/,
        #        infra/observability/, sibling uc{1,2,3}/agents/).
        #    10. Reproduction (NFR-003 — 3/3 cold deploys) — 10-step manual
        #        checklist with verification commands and expected output,
        #        ending with explicit Clément-deferral to STORY-028.
        #    11. Recovery procedure (Levels 1-4).
        #    12. Production disclaimer (5 trade-offs + production sketch).
        #    13. Author notes — 6 engineering-trace items + consolidated 8-item
        #        Sprint-3 retro queue.
        #    14. Cleanup.
        #    15. References.
        # - All 22 internal relative links resolve (Python audit clean).
        # - English throughout (NFR-009).
        # - No secrets in committed text (NFR-011): Secret/ConfigMap setup
        #   names env keys + describes placeholders; no literal credentials.
        # - Quentin self-author cluster smoke verified all README claims
        #   against live kagent-workshop kind cluster state:
        #     - artemis-mission-coordinator: Accepted=True + Ready=True
        #     - artemis-launch-pad-debugger (UC2 post-patch): Accepted=True
        #     - artemis-rover-telemetry-debugger: Accepted=True
        #     - artemis-bulb-mcp discoveredTools: list_bulbs + update_bulb
        #     - bridge Services in kagent ns (prometheus + grafana
        #       ExternalName).
        #     - UC1 artemis-mission-control-debugger still Accepted=False on
        #       cluster (pre-existing drift the README documents in §Author
        #       notes Sprint-3-retro item 3 — single `kubectl apply -f
        #       uc1/agents/` re-applies the repo's corrected spec).
        # - Beat 1 → Beat 2 cluster-side smoke validated under STORY-026;
        #   STORY-027 inherits that validation (the README's reproduction
        #   checklist steps 7 + 8a match STORY-026's transcript).
        #
        # No new findings beyond the upstream stories' — the README's §Author
        # notes consolidates 6 engineering items + the 8-item Sprint-3 retro
        # queue from STORY-018/023/024/025/026 + this story's authoring.
        #
        # Deferred AC (per recursive deferral chain):
        # - Cross-author cold-deploy by Clément (NFR-003 second leg + NFR-008
        #   sign-off on the README itself) → STORY-028 (M5 dry-run).
        # - End-to-end agent reasoning loop validation (live OpenAI creds +
        #   reachable light-manager + all 3 UC clusters simultaneously)
        #   → STORY-028.
        # The consolidated M5-entry plate for Clément is documented in
        # STORY-027.md §Technical Notes table — covers cross-author cold-deploys
        # for UC1/UC2/UC3/UC4 + NFR-008 reviews of every tour, README,
        # manifest, agent, MCP, observability artefact authored under the
        # OOO swap window.

  - sprint_number: 5
    milestone: "M5"
    goal: "Lock the artefacts: end-to-end dry-run signed off by both authors, blockers patched, repo tagged for the workshop."
    start_date: "2026-05-18"
    end_date: "2026-05-19"
    capacity_points: 12
    committed_points: 6
    completed_points: 6
    status: "completed"
    # STORY-028 + STORY-029 both implemented out-of-band on 2026-05-11 alongside
    # the Sprint 4 close-out. Clément remains OOO through workshop (2026-05-20);
    # the NFR-008 cross-author repro deferral chain that started accumulating
    # in STORY-014/017 closes here as N/A for the workshop ship. Quentin solo
    # for the final two stories (ninth + tenth swaps of the OOO window).
    # STORY-028 (3 pts, ninth swap): doc-only dry-run synthesis. Consolidates
    # every per-UC validation transcript into a single audit document; closes
    # the recursive deferral chain; produces a 7-item workshop-day risk queue
    # for STORY-029. No new cluster work (per scope decision — Quentin running
    # the four UCs again on the same local kind would produce strictly less
    # signal than the prior per-story transcripts already contain).
    # STORY-029 (3 pts, tenth swap — closes Sprint 5 + the project): patches
    # the must-fix UC2 latent gaps STORY-024 surfaced + the make kagent-install
    # idempotency STORY-018 flagged; documents workshop-infrastructure hand-offs
    # for UC1 agent re-apply + RemoteMCPServer reconcile race; freezes the repo
    # with git tag workshop-2026-05-20-freeze.
    stories:
      - story_id: "STORY-028"
        title: "Full dry-run end-to-end"
        nfrs: ["NFR-003", "NFR-010"]
        points: 3
        owner: "Quentin Rodic"  # re-attributed: joint → Quentin (ninth swap of
                                # the OOO window). Doc-only dry-run synthesis;
                                # NFR-008 cross-review CLOSES N/A for workshop
                                # ship per the convention's in-extremis carve-out
                                # (Clément OOO through workshop, fixed ship date).
        status: "completed"
        completion_date: "2026-05-11"
        actual_points: 3
        # Doc-only synthesis per scope decision. Output:
        # - Per-UC validation roll-up (5 sub-sections for UC0/UC1/UC2/UC3/UC4)
        #   summarising what was validated, by whom, on which cluster, with
        #   what outcome, and what remains outside the validated envelope.
        # - Recursive deferral chain closure declaration (18-row table of every
        #   swap-chain story's NFR-008 review status — all "not reviewed";
        #   workshop ships with explicit self-review-only posture).
        # - 7-item workshop-day risk queue handed to STORY-029, prioritised
        #   by participant-impact severity. Items #1 (UC2 manifest patches —
        #   HIGH, workshop blocker) and #3 (make kagent-install idempotency —
        #   LOW but easy) patched in STORY-029; items #2 (UC1 agent re-apply)
        #   and #6 (RemoteMCPServer reconcile race) handed to workshop-
        #   infrastructure as pre-flight responsibilities; items #4/#5/#7
        #   ship as-is (low severity, fully documented in per-UC READMEs).
        # docs/dry-run-2026-05-19.md absorbed into STORY-028.md per the
        # single-source-of-truth scope decision (no separate dated artefact).
      - story_id: "STORY-029"
        title: "M5 corrections + freeze + git tag"
        nfrs: ["NFR-005", "NFR-006"]
        points: 3
        owner: "Quentin Rodic"  # re-attributed: joint → Quentin (tenth and
                                # FINAL swap of the OOO window — closes Sprint 5
                                # + the project). NFR-008 status mirrors
                                # STORY-028: N/A for workshop ship.
        status: "completed"
        completion_date: "2026-05-11"
        actual_points: 3
        # All AC validated:
        # - UC2 manifest patches applied (4 surgical edits across 2 files):
        #     uc2/manifests/40-deployment.yaml: image :v1 → :v1.0.0 (real
        #       apogasa-registry-published tag); strategy.type: Recreate (avoids
        #       rollout-restart deadlock under default RollingUpdate).
        #     uc2/manifests/20-bootstrap-taint-job.yaml: image :1.31.0 → :latest
        #       (real published tag); shell /bin/bash → /bin/sh (busybox-based
        #       :latest has no /bin/bash); tolerations for the synthetic taint
        #       the Job itself applies (allows idempotent re-runs on a cluster
        #       where a previous UC2/UC4 cycle left the taint).
        # - UC2 tour.json fileEdits content re-synced byte-identically with
        #   the patched manifests (Beat 1 fileEdits[2] = bootstrap Job;
        #   fileEdits[4] = deployment — both re-embedded via the same script
        #   STORY-026 used for UC4 tour authoring). Other 3 fileEdits entries
        #   (namespace, RBAC, service) unchanged.
        # - Makefile kagent-install: helm install → helm upgrade --install
        #   (one-line patch). Verified idempotent on the live cluster —
        #   re-running yields STATUS: deployed, REVISION: 4 instead of the
        #   pre-patch "cannot re-use a name" error.
        # - UC1 agent re-apply on the live cluster verified as proof-of-fix
        #   for risk-queue item #2 (workshop-infrastructure responsibility):
        #   kubectl apply -f uc1/agents/ → artemis-mission-control-debugger
        #   reaches Accepted=True within ~30s (READY may still be False while
        #   the runtime Pod spins up; Accepted=True is the gating condition).
        # - make lint-manifests clean across all UCs + observability + MCP.
        # - make validate-tours clean across all 5 tours (uc0/uc1/uc2/uc3/uc4).
        # - Deployment notes for workshop-infrastructure documented in
        #   STORY-029.md (UC1 agent re-apply per-vCluster + RemoteMCPServer
        #   reconcile race wait-or-poke pattern).
        #
        # Pending in this commit:
        # - git tag workshop-2026-05-20-freeze on the final commit.
        # - git push to origin: deferred to explicit user-authorised action
        #   per the project's "executing actions with care" rule (push affects
        #   shared state — workshop-infrastructure pulls from origin/main).
        #
        # The freeze posture: no further commits to uc*/manifests/, uc*/agents/,
        # uc*/tour.json, uc*/README.md, apps/, mcp/, infra/, Makefile, schemas/,
        # docs/architecture-..md, docs/tour-content-conventions.md,
        # docs/artemis-naming.md after the freeze tag. Only docs typo fixes if
        # any surface post-tag.

  - sprint_number: 6
    milestone: "M6"
    goal: "Post-dry-run patch: clear the 2 blockers (UC3 dashboard data, UC4 env var prereq) and apply the 6 issues found in the 2026-05-05 walk-through (see docs/dry-run-journal-2026-05-05.md). Re-freeze artefacts for the 2026-05-20 workshop."
    start_date: "2026-05-06"
    end_date: "2026-05-15"
    capacity_points: 30
    committed_points: 26
    completed_points: 0
    status: "not_started"
    stories:
      - story_id: "STORY-036"
        title: "UC0 dashboard bridge step"
        epic: "EPIC-001"
        frs: ["FR-006"]
        nfrs: ["NFR-010"]
        points: 2
        owner: "Clément Raussin"
        status: "not_started"
        # Dry-run finding 2026-05-05 16:30, severity issue.
      - story_id: "STORY-037"
        title: "UC1 framing: name the user-visible symptom alongside the lore"
        epic: "EPIC-002"
        frs: ["FR-009"]
        nfrs: ["NFR-009"]
        points: 2
        owner: "Clément Raussin"
        status: "not_started"
        # Dry-run finding 2026-05-05 16:31, severity issue.
      - story_id: "STORY-038"
        title: "UC1 Beat 3 expansion: UI explore, kubectl parity, chat, verify, optional self-fix spike"
        epic: "EPIC-002"
        frs: ["FR-009"]
        nfrs: ["NFR-008"]
        points: 5
        owner: "Clément Raussin"
        status: "not_started"
        # Dry-run finding 2026-05-05 16:32, severity issue. Multi-step Beat 3 + spike on agent write capability.
      - story_id: "STORY-039"
        title: "UC2 Beat 3 CLI output: pipe to jq or refit"
        epic: "EPIC-002"
        frs: ["FR-011"]
        nfrs: ["NFR-010"]
        points: 2
        owner: "Quentin Rodic"
        status: "not_started"
        # Dry-run finding 2026-05-05 16:38, severity issue.
      - story_id: "STORY-040"
        title: "UC3 Grafana dashboard URL fix or port-forward step"
        epic: "EPIC-003"
        frs: ["FR-013"]
        nfrs: ["NFR-010"]
        points: 3
        owner: "Clément Raussin"
        status: "not_started"
        # Dry-run finding 2026-05-05 16:39, severity issue.
      - story_id: "STORY-041"
        title: "UC3 dashboard data pipeline (BLOCKER)"
        epic: "EPIC-003"
        frs: ["FR-012", "FR-013"]
        nfrs: ["NFR-003"]
        points: 5
        owner: "Clément Raussin"
        status: "not_started"
        # Dry-run finding 2026-05-05 16:40, severity BLOCKER. No data in Grafana panels; UC3 demo has no payoff.
      - story_id: "STORY-042"
        title: "Tour explanation density: convention rule + pass across all UCs"
        epic: "EPIC-001"
        frs: ["FR-006"]
        nfrs: ["NFR-009"]
        points: 5
        owner: "joint"
        status: "not_started"
        # Dry-run finding 2026-05-05 16:42 + 16:46, severity issue. Cross-cutting density pass.
      - story_id: "STORY-043"
        title: "UC4 WORKSHOP_PARTICIPANT_LOGIN prereq hardening (BLOCKER)"
        epic: "EPIC-004"
        frs: ["FR-016"]
        nfrs: ["NFR-010"]
        points: 2
        owner: "Quentin Rodic"
        status: "not_started"
        # Dry-run finding 2026-05-05 16:45, severity BLOCKER. `kmcp build` fails on first command without env var.

velocity:
  sprint_1: 24
  sprint_2: 20
  sprint_2_5: 14
  sprint_3: 25
  sprint_4: 14
  sprint_5: 6
  sprint_6: null
  rolling_average: 17

metrics:
  total_epics: 4
  total_stories: 42
  total_points: 131
  stories_completed: 33
  stories_in_progress: 0
  stories_skipped: 1
  stories_not_started: 8
