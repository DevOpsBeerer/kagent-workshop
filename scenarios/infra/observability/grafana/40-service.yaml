apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: artemis-observability
  labels:
    app: grafana
    kagent-workshop/component: observability
spec:
  type: ClusterIP
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
